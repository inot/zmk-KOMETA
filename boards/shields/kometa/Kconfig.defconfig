if SHIELD_KOMETA_LEFT

config ZMK_KEYBOARD_NAME
    default "kometa"

endif

if SHIELD_KOMETA_DONGLE

config ZMK_KEYBOARD_NAME
    default "kometa"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

config ZMK_SPLIT
    default y

config ZMK_SPLIT_BLE_CENTRAL_PERIPHERALS
    default 2

config BT_MAX_CONN
    default 7

config BT_MAX_PAIRED
    default 7

endif

if SHIELD_KOMETA_LEFT || SHIELD_KOMETA_RIGHT

config ZMK_SPLIT
    default y

config ZMK_BLE
    default y

endif
